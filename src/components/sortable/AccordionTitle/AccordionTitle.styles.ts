import { SxProps, Theme } from '@mui/material'

export const actionIconWrapperSx: SxProps<Theme> = {
    flexGrow: 0,
    marginRight: '8px',
}

export const titleValueSx: SxProps<Theme> = {
    flexGrow: 1,
    textWrap: 'auto',
}
